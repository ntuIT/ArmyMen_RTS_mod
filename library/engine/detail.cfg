//////////////////////////////////////////////////////////////////////////////
//
// Pandemic Studios
//

// Low world detail
World(0)
{
  CPU(0);

  Factor(0.25);
  MrmDist(10);
  MrmFactor(6);
  FarPlane(180);

  // Turn off features
  Weather(0);
  Movies(0);
  Shadows(0);
  GenericShadows(0);
}

// Medium world detail
World(1)
{
  CPU(425);

  Factor(0.5);
  MrmDist(30);
  MrmFactor(1);
  FarPlane(250);

  // Turn off features
  GenericShadows(0);
}

// High world detail
World(2)
{
  CPU(995);

  Factor(1.0);
  MrmDist(50);
  MrmFactor(1.0);
  FarPlane(300);
}

// Low texture detail
Texture(0)
{
  TexMem(0);
  SysMem(0);

  Reduce(1);
}

// High texture detail
Texture(1)
{
  TexMem(11);
  SysMem(64);

  Reduce(0);
}
